/*
for(var i = 0; i <= ds_list_size(othergamepad); i++)
{
	draw_set_halign(fa_left);
	draw_set_font(font0);
	if i == ds_list_size(othergamepad)
		draw_text(0, (i * 16), global.cont);
	else
		draw_text(0, (i * 16), othergamepad[| i]);
}
*/